# Compatibility matrix

The status terms are `supported`, `tested`, `planned`, and `unsupported`.
`Tested` means automated Zig coverage unless the evidence explicitly says the
check is manual; pinned upstream vectors are identified in the evidence.

| Capability | Status | Evidence or boundary |
| --- | --- | --- |
| Explicit format selection | Supported | Callers select `.v2` or `.v3`; both use `LTX1`, so no decoder auto-detection or retry is attempted |
| Decode Go v0.4.0 LTX v2 | Tested | Six regenerated byte-exact fixtures cover four-byte page headers, independent legacy LZ4 frames, page index, trailer, logical checksums, empty and no-checksum transitions, SQLite bytes, maximum page size, and near-lock page numbers; pinned oracle commit `2af9b0cb7a6eebfb59c2ca76acc4ae3adf4b6a09` |
| Decode current Go LTX v3 | Tested | Byte-exact snapshot, incremental, no-checksum, maximum-page, and near-lock fixtures |
| Decode Celld/Go v0.5.2 vector | Tested | Pinned byte-exact two-page, multi-index-entry fixture |
| Real Litestream capture chain | Tested | Immutable v0.5.11 L0 snapshot plus five incrementals from Celld; exact artifact and per-prefix database hashes, nonzero WAL metadata, legacy frames, and final SQLite integrity/row checks |
| Go decodes Zig output | Tested | Optional `zig build interop` uses the exact pinned Go module for a fresh snapshot, a five-chain 512–65,536-byte differential compaction matrix, the earlier synthetic cases, and a TX1–TX4 real-capture compaction; complete output bytes and decoded database hashes are exact |
| Real Litestream reads Zig compaction | Tested | Checksum-pinned v0.5.16 restores a current flagged Zig L1 object followed by the legacy-frame TX5/TX6 L0 tail with exact SQLite integrity and rows, plus the matrix's maximum-page no-checksum output with an exact image hash; its forced no-checksum restore predictably rejects the checked-growth matrix output's nonzero post checksum |
| Flagged raw LZ4 block frames | Tested | Zig reproduces complete pinned Go and Celld match-compressed files byte for byte |
| Normal compressed matches | Tested | Exact pierrec/Celld vectors cover overlap, extension lengths, workspace reset, and 512..65536-byte pages |
| Legacy unflagged v3 frames | Tested | Historical Go compressed and stored-block fixtures; canonical one-block 64 KiB profile |
| Staged apply from LTX v2 | Tested | A checksummed snapshot/incremental chain plus structured v2/v3 fuzz inputs enter the same private-stage, full-verification, final-image checksum, poisoning, and atomic publication protocol as v3 |
| SQLite generation publication from LTX v2 | Tested | A pinned valid SQLite snapshot publishes and opens through the generation store; the store persists the verified image and position rather than source framing |
| Mixed v2/v3 migration compaction | Tested | V2-only and mixed inputs require exact continuity and common checksum mode; complete canonical v3 output bytes match independently constructed current-Go outputs |
| Canonical encoding | Supported | Encoder output is v3-only with six-byte flagged raw-LZ4 pages; `.v2` encoding returns `UnsupportedFormatVersion` |
| LTX v1 | Unsupported | No decoder or encoder path; never inferred from `LTX1` or another version's layout |
| Snapshots | Tested | One-page and empty pinned Go fixtures; completeness enforced |
| Incremental transitions | Tested | Pinned Go two-page fixture plus strict order and checksum-continuity tests |
| No-checksum files | Tested | Pinned Go fixture; pre/post database checksums zero, file checksum required |
| Empty database transitions | Tested | Go fixture uses post checksum `0x8000000000000000` |
| Page-index validation | Tested | Exact entries, offsets, sizes, canonical varints, and byte size |
| Nonzero reserved header bytes | Supported | Accepted by structural header decoding; encoder emits zero |
| Trailing-byte rejection | Tested | Exact EOF required after the trailer |
| Truncation boundaries | Tested | Every strict prefix of all twelve bounded-page v2/v3 Go and Celld fixtures is rejected |
| Decoder, applier, and compactor fuzzing | Tested | Structured v2/v3 selection, checked-in valid and mixed corpora, deterministic mutations, bounded termination, exact error/state checks, transport equivalence, no premature publication, and terminal compactor states |
| Raw LZ4 fuzzing | Tested | Guarded decoder outputs through 65,536 bytes; deterministic fast and literal encoders round trip dirty-workspace inputs through 4,096 bytes |
| Position contiguity | Tested | Exact TXID and enabled-checksum equality |
| Staged apply orchestration | Tested | Private staging, explicit transition modes, page-size compatibility, full-image checksum verification, abort-on-error, atomic publish contract, exact reconstruction of all six real Litestream capture prefixes, and sequential-versus-compacted apply equality across the five-chain matrix |
| Compaction | Tested | Allocation-free oldest-to-newest merge; current and legacy inputs, exact TXID/checksum continuity, consistent checksum mode, newest-page precedence, final-commit shrink and deletion, zeroed source metadata, current-profile output, caller bounds, partial-output rejection, and five-case pinned-Go byte equality |
| SQLite WAL capture | Tested | Allocation-free WAL parsing and page selection, empty/zero-commit snapshot fallback, live-workspace alias rejection, monotonic timestamp enforcement, byte/age/frame checkpoint policy with verified PASSIVE completion and deferred retry state, crash-boundary replay, live SQLite integration, and direct transactional publication with no whole-object output buffer |
| Filesystem object access and publication | Tested | Exact-size listings, positional exact reads, bounded one-byte through deployment-sized sequential reader windows, listed-size change detection, collision-safe exclusive temporary-file sessions, checked sequential writes, file/complete-directory-chain sync, post-rename indeterminacy, abort cleanup, and the backend-agnostic conformance suite |
| S3-compatible object access and publication | Tested | Exact-size pagination with per-page and total-page bounds, signed single-range GETs with strict `206 Content-Range` validation, SigV4, injected TLS time, bounded retry, Litestream timestamp metadata, automatic single-request-to-multipart streaming, checked completion XML, retryable abort cleanup, conditional-write indeterminacy, MinIO integration, and the backend-agnostic conformance suite |
| Synchronous replication controller | Tested | Empty, verified-local, and sidecar-free restore-latest startup; copied configuration, encoder/resource and bounded-read preflight, capture and trusted position; identity-checked all-level restore; adjacent-level compaction over disjoint fixed windows; selected-source publish-before-delete retention; caller-error versus processing-failure poisoning; finish semantics; live SQLite lifecycle; and zero-buffer transactional publication |
| Quiescent SQLite generation store | Tested | Optional `ltx_sqlite`; explicit state/failure inspection, durable empty baseline, host-owned connection gate, canonical immutable read-only URI, sidecar rejection, generation/epoch overflow, exclusive indeterminate recovery, synchronized cross-process shared-lease contention and abrupt abandonment, retained and stale-epoch lease qualification, and SQLite-produced checksummed A-to-B-to-C crash replay at every publication durability boundary with growth, shrink, slot reuse, exact active/retained bytes, and semantic integrity checks |
| Fixed-path replacement under open SQLite handles | Unsupported | Unsafe pathname/journal association; exact Online Backup output differs from verified LTX bytes |

The encoder uses a bounded port of the fast raw-block compressor pinned by Go
and independently ported by Celld. With the canonical output bound it preserves
their exact match choices and bytes. A deliberately smaller but still safe
configured cap selects deterministic literal-only blocks. The decoder accepts
both forms. Legacy decoding is strictly scoped to the independent 64 KiB,
content-checksummed frame profile emitted by upstream; other standard LZ4 frame
options return `UnsupportedPageEncoding`.

The stronger v3 validation policy also applies to v2 import: canonical page
order, snapshot completeness, one-to-one page-index correspondence, canonical
varints, trailer validation, configured bounds, checksums, and exact EOF are
not relaxed merely because the older Go decoder accepted less canonical input.
Celld remains a secondary v3 interoperability and deployment reference and has
no v2 implementation or v2 corpus.
